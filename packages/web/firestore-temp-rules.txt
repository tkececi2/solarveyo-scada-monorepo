// GEÇİCİ BASİT KURALLAR - Test için
// Firebase Console → Firestore Database → Rules'a yapıştır

rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Geçici olarak tüm işlemlere izin ver
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
