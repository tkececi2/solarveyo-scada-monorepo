rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Basit test kuralları - sadece giriş yapmış kullanıcılar
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
